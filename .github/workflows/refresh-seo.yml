name: Refresh SEO
on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/refresh-seo.yml"
      - "scripts/refresh-sitemap.mjs"
      - "scripts/submit-indexnow.mjs"
      - "seo.css"
      - "21d6e780c8c007515162b511ae98a991668a603d437c5119.txt"
  schedule:
    - cron: "17 * * * *"
  workflow_dispatch:
permissions:
  contents: write
jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: "24"
      - name: Rebuild search pages and sitemaps
        run: node scripts/refresh-sitemap.mjs
      - name: Detect SEO changes
        id: changes
        shell: bash
        run: |
          if [ -z "$(git status --porcelain -- cases people topics case-archive people.html topics.html all-cases.html criminal.html civil.html administrative.html acquittals.html sentencing.html sitemap.xml sitemap.txt sitemaps)" ]; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Notify IndexNow
        if: github.event_name == 'push' || steps.changes.outputs.changed == 'true'
        run: node scripts/submit-indexnow.mjs
      - name: Commit generated SEO changes
        if: steps.changes.outputs.changed == 'true'
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add cases people topics case-archive people.html topics.html all-cases.html criminal.html civil.html administrative.html acquittals.html sentencing.html sitemap.xml sitemap.txt sitemaps
          git commit -m "Refresh static search pages"
          git fetch origin main
          git rebase origin/main
          git push origin main
